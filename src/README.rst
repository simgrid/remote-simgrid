Remote SimGrid sources
======================

Arborescence
------------

- `rsg` contains code specific to the command-line tool (and server) `rsg`
- `librsg` contains code specific to the client library
- `common` contains code that is not specific to any subproject
